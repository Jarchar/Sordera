# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.PalabraController.index()
# An example controller showing how to use dependency injection
GET     /palabras                      controllers.PalabraController.list(p:Int ?= 0, s ?= "nombre", o ?= "asc", f ?= "")

# Add computer
GET     /palabras/nueva             controllers.PalabraController.crear()
POST    /computers                  controllers.PalabraController.salvar()

# Edit existing computer
GET     /palabras/:id              controllers.PalabraController.editar(id:Long)
POST    /palabras/:id              controllers.PalabraController.actualizar(id:Long)

# Delete a computer
POST    /palabras/:id/borrar       controllers.PalabraController.borrar(id:Long)

# Map static resources from the /public folder to the /assets URL path
GET     /palabras/*file               controllers.Assets.at(path="/public", file)
